#include <stdio.h>
#include "main.h"

/**
 * main - multi numbers
 * @argc: argum
 * @argv: array
 * Return: 0
 */

int main(int argc, char *argv[])
{
int x, y;
if (argc < 3)
{
printf("Error\n");
return (1);
}
x = atoi(argv[1]);
y = atoi(argv[2]);
printf("%d\n", result);
return (0);
}
